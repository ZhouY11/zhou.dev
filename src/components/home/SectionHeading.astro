---
interface Props {
  eyebrow: string;
  title: string;
  description?: string;
}

const { eyebrow, title, description } = Astro.props;
---

<header class="max-w-2xl">
  <p class="text-sm font-medium tracking-widest text-brand uppercase">
    {eyebrow}
  </p>

  <h2 class="mt-3 text-3xl font-semibold tracking-tight text-fg sm:text-4xl">
    {title}
  </h2>

  {description && <p class="mt-4 leading-7 text-fg-muted">{description}</p>}
</header>
